var x: int = (1 + 2) / 4;

def main (argc: int, argv: string*): int = {
  var x: bool = true | false;
  var a: int = 4 * -1;
  var b: int = 1 || 2;
  var s: string = "hello";
  var t: string = s;
  var ch: char = 'h';
  var n: int* = null;
  printf("Hello world!\n");

  while (x < 1) {
    t += 2;
    continue;
  };

  if (x < 1) {
    t = 2;
  };

  t = s = r;
  return 14.1;
}

struct String {
  var s: string = null;

  def initialize (self: String*, s: string): String = {
    
  }

}

